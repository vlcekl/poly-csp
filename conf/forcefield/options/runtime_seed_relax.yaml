# @package _global_
defaults:
  - /forcefield/options/runtime_relax@forcefield.options
  - override /seed_bias: solvent_ready
  - _self_

forcefield:
  options:
    hbond_k: 0.0
    dihedral_l: 0.0

    # Seed-mode soft finalization is intentionally incompatible with annealing.
    anneal:
      enabled: false
