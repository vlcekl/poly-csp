# poly_csp

**poly_csp** is a deterministic construction framework for building **highly ordered chiral stationary phase (CSP) polymers** for molecular dynamics simulations.

The primary target system is:

> **Chiralpak AD–like amylose tris(3,5-dimethylphenylcarbamate) (ADMPC)**

The framework builds:

* Helically symmetric polysaccharide backbones (amylose, cellulose)
* Fully functionalized selectors at C2/C3/C6
* Deterministic, symmetry-consistent initial conformations
* Pre-organized selector orientations suitable for MD

The design goal is to **avoid kinetically trapped, disordered coatings** during equilibration by enforcing:

* Screw-axis symmetry from the start
* Deterministic selector placement
* Explicit control over repeat units
* Optional hydrogen-bond network pre-organization
* Symmetry-preserving relaxation protocols

---

# Scientific Motivation

Traditional workflows:

1. Build polymer.
2. Attach bulky selectors.
3. Minimize/anneal.
4. Hope the system finds an ordered state.

This frequently fails for coated CSPs because:

* Selector torsions create a rugged energy landscape.
* Carbamate H-bond networks are not formed spontaneously.
* MD gets trapped in local minima.
* The resulting phase is disordered.

**poly_csp instead constructs the ordered helix directly**, then relaxes under symmetry-preserving constraints.

---

# Core Design Philosophy

The system is built in stages:

1. **Helical symmetry imposed at construction**

   * Polymer is generated by applying a screw transform per residue.
   * No arbitrary oligomer conformer sampling.

2. **Deterministic selector placement**

   * Selectors are attached in residue-local frames.
   * Initial torsions are explicitly controlled.

3. **Repeat-unit optimization**

   * Only a small helical repeat is optimized.
   * The repeat is replicated by symmetry.

4. **Restraint-guided relaxation**

   * Backbone symmetry restraints
   * Selector dihedral restraints
   * Optional hydrogen bond restraints
   * Gradual release before production MD

---

# Project Structure

```
poly_csp/
│
├── README.md
├── pyproject.toml
│
├── conf/                  # Hydra configuration files
│   ├── config.yaml
│   ├── helix/
│   ├── polymer/
│   ├── selector/
│   ├── ordering/
│   ├── relax/
│   └── qc/
│
└── src/
    └── poly_csp/
        ├── __init__.py
        ├── config/
        │   ├── schema.py
        │   └── presets.py
        │
        ├── geometry/
        │   ├── transform.py
        │   ├── local_frames.py
        │   └── dihedrals.py
        │
        ├── chemistry/
        │   ├── monomers.py
        │   ├── backbone_build.py
        │   ├── polymerize.py
        │   ├── terminals.py
        │   ├── selectors.py
        │   └── functionalization.py
        │
        ├── ordering/
        │   ├── rotamers.py
        │   ├── scoring.py
        │   ├── hbonds.py
        │   └── symmetry_opt.py
        │
        ├── mm/
        │   ├── openmm_system.py
        │   ├── restraints.py
        │   ├── anneal.py
        │   └── minimize.py
        │
        ├── pipelines/
        │   └── build_csp.py
        │
        └── io/
            ├── rdkit_io.py
            ├── amber.py
            └── pdb.py
```

Runtime source of truth:
- Build-time defaults and behavior are controlled by Hydra YAML in `conf/`.
- `src/poly_csp/config/presets.py` is reference/experimental and not the active runtime default path.

---

# Dependencies

Core stack:

* Python ≥ 3.10
* RDKit
* NumPy / SciPy
* Pydantic v2
* OpenMM
* Hydra
* AmberTools (for `antechamber`, `parmchk2`, `tleap` AMBER backend)

Optional (later stages):

* OpenFF / GAFF / CGenFF parameterization tools

---

# Hydra Configuration

Configuration is handled via **Hydra**.

All configuration files live in:

```
/conf/
```

Example structure:

```
conf/
├── config.yaml
├── helix/
│   ├── admcp_chiralpak_ad.yaml
│   └── cellulose_i.yaml
├── polymer/
│   ├── amylose.yaml
│   └── cellulose.yaml
├── selector/
│   └── dmpc_35.yaml
└── relax/
    └── restrained.yaml
```

### Example `conf/config.yaml`

```yaml
defaults:
  - helix: admcp_chiralpak_ad
  - polymer: amylose
  - selector: dmpc_35
  - relax: restrained
  - _self_

polymer:
  dp: 24

output:
  dir: outputs/
```

---

# Default Target: Chiralpak AD–Like ADMPC

The default helix preset:

* Left-handed 4/3 helix
* Repeat: 4 residues, 3 turns
* Rise per residue ≈ 3.7 Å
* Deterministic screw-axis construction

This corresponds to literature-reported conformations for amylose tris(3,5-DMPC).

---

# Basic Usage

## 1. Install

```bash
conda env create -f environment.yml
conda activate polycsp
pip install -e .
```
Verify AMBER backend tools are available:

```bash
which antechamber
which parmchk2
which tleap
```

---

## 2. Build a CSP polymer

Example:

```bash
python -m poly_csp.pipelines.build_csp polymer.dp=24
```

Hydra will create an output directory like:

```
outputs/2026-03-01/00-00-00/
```

Containing:

* `model.pdb`
* `build_report.json`
* optional logs

---

## 3. Override parameters

Change helix:

```bash
python -m poly_csp.pipelines.build_csp helix=cellulose_i
```

Change degree of polymerization:

```bash
python -m poly_csp.pipelines.build_csp polymer.dp=48
```

Change selector site(s):

```bash
python -m poly_csp.pipelines.build_csp selector.sites=[C6]
```

---

# Output Products

Each build produces:

* Fully connected RDKit polymer
* Assigned 3D coordinates
* Deterministic helical symmetry
* Optional selector torsion initialization
* QC metrics:

  * Symmetry RMSD
  * Clash score
  * Torsion summary

---

# Model Validity

The project supports two distinct relaxation models:

1. `geometry_pre_relax` (default):
   - Soft-repulsion + restraint guided coordinate cleanup.
   - Useful for deterministic pre-organization and clash reduction.
   - Not a full physical force-field model.

2. `ambertools_parameterized`:
   - Uses AmberTools-generated `prmtop/inpcrd` artifacts for parameterized OpenMM minimization.
   - Intended for physically grounded relaxation workflows.

Use the mode explicitly via `relax.mode=...` and interpret outputs accordingly.

---

# Quality Control Philosophy

Each build should satisfy:

* Low screw-symmetry RMSD
* Acceptable steric overlap
* Reproducibility (no random conformer generation)

The pipeline is deterministic by design.

---

# Development Strategy

Recommended implementation order:

1. Helix + backbone replication
2. Polymer graph construction
3. Single-site selector attachment
4. Multi-site functionalization
5. Rotamer + H-bond pre-organization
6. OpenMM restrained relaxation
7. Full CSP tiling / surface attachment

---

# Roadmap

Planned extensions:

* Hydrogen-bond network scoring
* Symmetry-aware rotamer optimization
* Silica surface coating module
* Multi-chain packing builder
* Force-field abstraction layer
* Automated validation against experimental CD/NMR constraints

---

# Design Constraints

* No stochastic conformer generation during build.
* Screw symmetry must be preserved unless explicitly relaxed.
* Geometry operations must be testable and unit-tested.
* All transforms must be explicit matrices + translations.

---

# Contributing

When adding new selectors:

1. Implement `SelectorTemplate`.
2. Define attachment atom index.
3. Define dihedral set.
4. Register in `SelectorRegistry`.
5. Add Hydra config in `/conf/selector/`.

When adding new helix presets:

1. Define `HelixSpec` in `presets.py`.
2. Add Hydra YAML in `/conf/helix/`.

---

# Citation / Scientific Context

This project is designed for computational modeling of coated polysaccharide CSPs such as:

* Amylose tris(3,5-dimethylphenylcarbamate) (ADMPC)
* Cellulose tris(3,5-dimethylphenylcarbamate) (CDMPC)

Used in chiral chromatography systems such as Chiralpak AD.

---

# Status

Deterministic construction workflow with selector attachment, ordering, QC, and two relaxation modes (geometric pre-relax and AmberTools-parameterized).
